<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DELTA Loading Screen</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
            color: #fff;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }
        
        .container {
            width: 90%;
            max-width: 800px;
            background: rgba(0, 0, 0, 0.7);
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 0 30px rgba(0, 150, 255, 0.5);
            position: relative;
            overflow: hidden;
        }
        
        .container::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(
                45deg,
                transparent, 
                rgba(0, 150, 255, 0.1), 
                transparent
            );
            transform: rotate(45deg);
            animation: shine 6s infinite;
            z-index: 0;
        }
        
        @keyframes shine {
            0% { left: -50%; }
            100% { left: 150%; }
        }
        
        header {
            text-align: center;
            margin-bottom: 30px;
            position: relative;
            z-index: 1;
        }
        
        h1 {
            font-size: 2.8rem;
            margin-bottom: 10px;
            color: #ff4d4d;
            text-shadow: 0 0 15px rgba(255, 77, 77, 0.7);
            letter-spacing: 1px;
        }
        
        .status {
            font-size: 1.2rem;
            color: #ff9966;
            margin-bottom: 30px;
            text-align: center;
        }
        
        .requirements {
            background: rgba(0, 30, 60, 0.5);
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 30px;
            position: relative;
            z-index: 1;
        }
        
        .requirement {
            padding: 15px;
            margin: 15px 0;
            background: rgba(0, 50, 100, 0.3);
            border-radius: 8px;
            display: flex;
            align-items: center;
            transition: all 0.3s ease;
            border-left: 4px solid #0099ff;
        }
        
        .requirement:hover {
            background: rgba(0, 80, 160, 0.4);
            transform: translateX(5px);
        }
        
        .number {
            display: inline-flex;
            justify-content: center;
            align-items: center;
            width: 30px;
            height: 30px;
            background: #0099ff;
            color: white;
            border-radius: 50%;
            margin-right: 15px;
            flex-shrink: 0;
        }
        
        .progress-container {
            height: 20px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            overflow: hidden;
            margin: 30px 0;
            position: relative;
            z-index: 1;
        }
        
        .progress-bar {
            height: 100%;
            width: 0%;
            background: linear-gradient(90deg, #ff9966, #ff5e62);
            border-radius: 10px;
            transition: width 0.5s ease;
        }
        
        .action-buttons {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
            position: relative;
            z-index: 1;
        }
        
        button {
            padding: 12px 25px;
            border: none;
            border-radius: 8px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 1rem;
        }
        
        #fix-btn {
            background: linear-gradient(135deg, #ff9966, #ff5e62);
            color: white;
        }
        
        #fix-btn:hover {
            background: linear-gradient(135deg, #ff5e62, #ff9966);
            box-shadow: 0 0 15px rgba(255, 94, 98, 0.5);
        }
        
        #restart-btn {
            background: rgba(255, 255, 255, 0.1);
            color: #0099ff;
            border: 1px solid #0099ff;
        }
        
        #restart-btn:hover {
            background: rgba(0, 153, 255, 0.2);
            box-shadow: 0 0 15px rgba(0, 150, 255, 0.5);
        }
        
        .pulse {
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0% { opacity: 0.7; }
            50% { opacity: 1; }
            100% { opacity: 0.7; }
        }
        
        .logo {
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 20px;
            color: #0099ff;
            text-shadow: 0 0 10px rgba(0, 153, 255, 0.7);
            letter-spacing: 2px;
        }
        
        footer {
            text-align: center;
            margin-top: 30px;
            font-size: 0.9rem;
            color: rgba(255, 255, 255, 0.6);
            position: relative;
            z-index: 1;
        }
        
        @media (max-width: 600px) {
            h1 {
                font-size: 2rem;
            }
            
            .container {
                padding: 20px;
            }
            
            .action-buttons {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo">DELTA</div>
        <header>
            <h1>SYSTEM WARNING</h1>
            <div class="status pulse">DELTA IS NOT WORKING</div>
        </header>
        
        <div class="requirements">
            <p>To get DELTA working, you need to:</p>
            
            <div class="requirement">
                <span class="number">1</span>
                <span class="text">Turn Off your Anti Cheat</span>
            </div>
            
            <div class="requirement">
                <span class="number">2</span>
                <span class="text">Disable Verify Teleport</span>
            </div>
            
            <div class="requirement">
                <span class="number">3</span>
                <span class="text">Disable Anti Scam</span>
            </div>
        </div>
        
        <div class="progress-container">
            <div class="progress-bar" id="progress-bar"></div>
        </div>
        
        <div class="action-buttons">
            <button id="fix-btn">AUTO FIX</button>
            <button id="restart-btn">RESTART DELTA</button>
        </div>
        
        <footer>
            <p>DELTA System © 2023 | Secure Environment Loader</p>
        </footer>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const progressBar = document.getElementById('progress-bar');
            const fixBtn = document.getElementById('fix-btn');
            const restartBtn = document.getElementById('restart-btn');
            const status = document.querySelector('.status');
            
            // Simulate progress
            let progress = 0;
            const interval = setInterval(() => {
                progress += Math.random() * 5;
                if (progress >= 100) {
                    progress = 100;
                    clearInterval(interval);
                }
                progressBar.style.width = progress + '%';
            }, 500);
            
            // Fix button handler
            fixBtn.addEventListener('click', function() {
                status.textContent = "Fixing DELTA system...";
                status.classList.add('pulse');
                
                // Simulate fixing process
                setTimeout(() => {
                    status.textContent = "Anti Cheat disabled!";
                    setTimeout(() => {
                        status.textContent = "Verify Teleport disabled!";
                        setTimeout(() => {
                            status.textContent = "Anti Scam disabled!";
                            setTimeout(() => {
                                status.textContent = "DELTA IS NOW WORKING!";
                                status.style.color = "#66ff66";
                                status.classList.remove('pulse');
                            }, 800);
                        }, 800);
                    }, 800);
                }, 1500);
            });
            
            // Restart button handler
            restartBtn.addEventListener('click', function() {
                status.textContent = "Restarting DELTA...";
                status.classList.add('pulse');
                
                // Simulate restart
                setTimeout(() => {
                    progress = 0;
                    progressBar.style.width = '0%';
                    
                    const newInterval = setInterval(() => {
                        progress += Math.random() * 8;
                        if (progress >= 100) {
                            progress = 100;
                            clearInterval(newInterval);
                            status.textContent = "DELTA restart completed!";
                            status.classList.remove('pulse');
                        }
                        progressBar.style.width = progress + '%';
                    }, 300);
                }, 2000);
            });
        });
    </script>
</body>
</html>
